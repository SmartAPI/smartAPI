biothings[web_extra]==0.5.0

# document validation
jsonschema>=2.6.0

# web handling
elasticsearch>=7, <8
elasticsearch-dsl>=7, <8
torngithub==0.2.0 
Jinja2>=2.9.6
pycurl==7.43.0.3

# gitdb version specified because gitdb.utils.compat not available in newest version
gitdb==0.6.4

# s3 backup
boto3>=1.9.86,<2.0
